/* Background */
.flex .ntb-calendar {
	width: 180px;
	height: 192px;
	text-align: center;
    font-weight: normal;
	background: url(assets/background.gif) no-repeat left top;
}

/* General */
.flex .ntb-calendar-container a {
    text-decoration: none;
}
.flex .ntb-calendar-monthheader td,
.flex .ntb-calendar-header td,
.flex .ntb-calendar-day,
.flex .ntb-calendar-dayheader {
	text-align: center;
    vertical-align: middle;
}
.flex .ntb-calendar-container {
    font-family: Helvetica,Arial,Verdana,sans-serif;
    font-size: 11px;
}

/* Container & Basic font */
.flex .ntb-calendar-container {
	-moz-user-select: none;
    -khtml-user-select: none;
    user-select: none;
	background: #bcc6cb;
	border: 1px solid #c3cfd5;
}

/* Header (Multi) */
.flex .ntb-calendar-header {
	height: 24px;
	background: url(assets/top_header.png) bottom left repeat-x;
}
.flex .ntb-calendar-title {	
	color: #0b333c;
	font-size: 12px;
	cursor: pointer;
}

/* Header (Month) */
.flex .ntb-calendar-monthheader {
    height: 23px;
}
.flex a.ntb-calendar-month, .flex a.ntb-calendar-year {
    color: #0b333c;
	font-size: 12px;
}

/* Header Buttons */
.flex a.ntb-calendar-prev, .flex a.ntb-calendar-next {
    color: #4a4a4a;
	font-size: 0;
	height: 21px;
	width: 21px;
	display: block;
	margin: 3px 4px 0 4px;
	background: url(assets/prev.png) top left no-repeat;
	float: left;

}
.flex a.ntb-calendar-next {
	background: url(assets/next.png) top left no-repeat;
	float: right;
}

.flex a.ntb-calendar-nextdis {
	background: url(assets/next_dis.png) top left no-repeat;
	float: right;
	cursor: default;
}

.flex a.ntb-calendar-prevdis {
	background: url(assets/prev_dis.png) top left no-repeat;
	float: left;
	cursor: default;
}

/* Days (Week) */
.flex .ntb-calendar-dayheader {
	height: 25px;
}

/* Days */
.flex .ntb-calendar-day {
    cursor: pointer;
    height: 16px;
	line-height: 16px;
	margin: 1px 2px;
	border: 1px solid #fff;
	color: #0b333c;
}
.flex a.ntb-calendar-thismonth:hover {
	background: #b2e1ff;
}
.flex .ntb-calendar a.ntb-calendar-disabled {
	color: #aaa;
	cursor: default;
}
.flex .ntb-calendar a.ntb-calendar-event {
	border: 1px solid #7fceff;
}
.flex .ntb-calendar a.ntb-calendar-currentday {
	background: #7fceff;
}

/* Days (Last & Nex Month) */
.flex .ntb-calendar-lastmonth, .flex .ntb-calendar-nextmonth {
	color: #bbb;
	cursor: default;
}

/* Text Input */
.flex .ntb-inputcontainer {
	border: 1px solid #dddddd;
	border-top-color: #7c7c7c;
	border-left-color: #c3c3c3;
	font-size: 11px;
	width: 150px;
}

.flex input.ntb-dateinput {
	height: 18px;
	padding: 0px;
	border: 0px;
	margin: 2px 0px 0px 2px;
	width: 140px;
	background-color: inherit;
}

.flex input.ntb-dateinput:focus {
}
.flex input.ntb-dateinput-empty {
	background:#333444;
}

.flex .ntb-invalid {
	font-weight: bold;
	color: #aaa;
	background-color: #ddd;
	border: 1px solid #7fceff;
	font-size: 11px;
	width: 150px;
}

/* Calender Button */
.flex .ntb-calendar-button {
	height: 22px;
	width: 25px;
	background: transparent url(assets/button.gif) top left no-repeat;
	cursor: pointer;
	margin-left: 2px;
}

/* Chooser Navigation */
.flex .ntb-calendar-navcontainer {
	border: 1px solid #a1a1a1;
	border-top: none;
	width: 150px;
    background: #f0f0f0;
}
.flex .ntb-calendar-navcontainer label {
	display: block;
}
.flex .ntb-calendar-navcontainer select, .flex .ntb-calendar-navcontainer input, .flex .ntb-calendar-navcontainer button {
	font-size: 11px;
}
.flex .ntb-calendar-navcontainer button {
	margin-right: 8px;
}
.flex .ntb-calendar-navcontainer .ntb-calendar-monthcontainer,
.flex .ntb-calendar-navcontainer .ntb-calendar-yearcontainer,
.flex .ntb-calendar-navcontainer .ntb-calendar-controls {
	padding: 5px 15px;	
}
.flex .ntb-calendar-navcontainer .ntb-calendar-controls {
	padding-bottom: 10px;
}
.flex .ntb-calendar-navcontainer .ntb-calendar-navms {
	width: 120px;
}